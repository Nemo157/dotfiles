# This allows pbcopy, pbpaste and other utilities that interact with the current
# user session to work on OS X

if-shell 'type >/dev/null 2>&1 reattach-to-user-namespace' 'set -g default-command "reattach-to-user-namespace -l zsh"'

# vim: ft=tmux
