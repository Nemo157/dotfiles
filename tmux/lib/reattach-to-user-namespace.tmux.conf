# This allows pbcopy, pbpaste and other utilities that interact with the current
# user session to work on OS X

if-shell 'reattach-to-user-namespace -v >&/dev/null' 'set -g default-command "reattach-to-user-namespace -l zsh"'

# vim: ft=tmux
