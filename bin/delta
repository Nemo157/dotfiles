#!/bin/sh

delta=$(which -a delta | tail -n -1 | head -n 1)

exec $delta \
    --theme ansi-dark \
    --plus-color black \
    --minus-color black \
    --plus-emph-color black \
    --minus-emph-color black \
    --commit-style plain \
    --file-style box \
    --hunk-style plain \
    --keep-plus-minus-markers \
    "$@"
